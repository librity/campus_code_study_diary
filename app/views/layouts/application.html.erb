<!DOCTYPE html>
<html>
  <head>
    <title>Study on Rails 🧑‍🎓</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= favicon_link_tag 'favicon.ico' %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
  </head>

  <body>
    <%= render 'shared/nav' %>
    <%= render 'shared/flash' %>
    <main class="main_container">
      <%= yield %>
    </main>
    <%= render 'footer' %>
  </body>
</html>
